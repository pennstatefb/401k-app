import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.georgesmith.retirementplanner',
  appName: '7-Year Runway',
  webDir: 'dist'
};

export default config;
